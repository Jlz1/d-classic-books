<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_main.xml - Main Activity Layout

    Layout utama aplikasi dengan:
    - DrawerLayout untuk navigation drawer
    - Hamburger menu untuk membuka drawer
    - Main content area untuk konten utama
    - Navigation drawer include (layout_navbar.xml)

    Colors:
    - Background: Primary (#0E0911)
    - Icons: Tertiary (#BC8F4B)
-->
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.MainActivity">

    <!-- Main Content -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/primary">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/source_code_pro"
            android:text="Main Content Area"
            android:textColor="@color/text"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Hamburger Icon to open drawer -->
        <ImageView
            android:id="@+id/btn_open_drawer"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_margin="16dp"
            android:contentDescription="@string/books_title"
            android:padding="12dp"
            android:src="@android:drawable/ic_menu_sort_by_size"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/tertiary" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Navigation Drawer -->
    <include
        android:id="@+id/drawer_start"
        layout="@layout/layout_navbar" />

</androidx.drawerlayout.widget.DrawerLayout>
